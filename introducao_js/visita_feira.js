let line = gets().split(" "); // Pega os valores digitados pelo usuário, tira os espaços e gera um array
let A = parseInt(line[0]); // Pega o primeiro valor do array 'line' e transforma em int
let B = parseInt(line[1]); // Pega o segundo valor do array 'line' e transforma em int
let total = A + B; // Calcula a soma de A e B e armazena na variável total
console.log("X = " + total); // Exibe na tela "X= " e o valor de 'total'